# Commands

Publishable command recipes live at the repo root in `commands/` (4 definitions: `add-tests.md`, `install-conductor.md`, `pr-create.md`, `simplify.md`), mirroring the user's global command install. See `../../AGENTS.md` for how they relate to the rest of the catalog.
