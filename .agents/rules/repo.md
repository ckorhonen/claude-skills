# Repo Rules

- Treat `AGENTS.md` as the canonical instruction file.
- Re-check the README, skill folders, and hook configuration before changing documented workflows.
- Preserve existing `.claude/hooks/` behavior.
